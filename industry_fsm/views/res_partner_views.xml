<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_address_form_industry_fsm" model="ir.ui.view">
        <field name="name">res.partner.form.address.fsm</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_address_form"/>
        <field name="mode">primary</field>
        <field name="priority" eval="25"/>
        <field name="arch" type="xml">
            <field name="city" position="attributes">
                <attribute name="required">1</attribute>
            </field>
            <field name="country_id" position="attributes">
                <attribute name="required">1</attribute>
            </field>
            <field name="website" position="replace"/>
            <field name="image_1920" position="replace"/>
            <xpath expr="//form" position="inside">
                <footer>
                    <button name="action_partner_navigate" type="object" string="Navigate To" class="btn btn-primary" data-hotkey="q" close="1"/>
                    <button string="Cancel" class="btn btn-secondary" special="cancel" data-hotkey="z"/>
                </footer>
            </xpath>
        </field>
    </record>

    <!-- Add Visites page to base partner form -->
    <record id="view_partner_form_inherit_visites" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.visites</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- Add Visites page to notebook -->
            <xpath expr="//notebook" position="inside">
                <page string="Visites" name="visites_page">
                    <group string="Informations de base">
                        <group>
                            <field name="potential" string="Potentiel"/>
                            <field name="region" string="Région"/>
                        </group>
                        <group>
                            <field name="responsible_delegue" string="Délégué responsable"/>
                            <field name="frequence_visite" string="Fréquence de visite mensuelle"/>
                        </group>
                    </group>
                    <group string="Statistiques des visites">
                        <group>
                            <field name="monthly_visits_count" string="Visites planifiées ce mois" readonly="1"/>
                        </group>
                        <group>
                            <!-- Placeholder for future visit statistics -->
                        </group>
                    </group>
                    <separator string="Historique des visites"/>
                    <field name="task_ids" domain="[('is_fsm', '=', True)]" context="{'default_partner_id': active_id, 'default_is_fsm': True}">
                        <tree string="Visites FSM" decoration-info="not stage_fold" decoration-success="stage_fold">
                            <field name="name"/>
                            <field name="stage_id"/>
                            <field name="stage_fold" invisible="1"/>
                            <field name="user_ids" widget="many2many_tags"/>
                            <field name="planned_date_begin"/>
                            <field name="planned_date_end"/>
                            <field name="priority" widget="priority"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>




</odoo>
